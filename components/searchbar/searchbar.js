import styles from "./searchbar.module.css";

export default function Searchbar({ location }) {

    const search = (e) => {
        
    }

    return (
        <input className={styles.searchbar}>Type in a location</input>
    )
}